---
title: Executor Deployment
description: How the executor connects to Convex and runs revision-scoped jobs.
---

The executor is the service that actually runs TokenSpace jobs. It subscribes to the backend for runnable work, claims jobs, and executes them against a compiled revision plus session state.

TODO: Executor deployment guide