## Admin Settings

- Treat `/workspace/$slug/admin/settings` as a stable **General** page.
- Do not add new product settings sections to General by default.
- Add new settings to a dedicated admin route/page (for example `/workspace/$slug/admin/<feature>`) and link it from the admin sidebar unless instructed otherwise.
