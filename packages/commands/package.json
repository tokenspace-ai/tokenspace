{
  "name": "@tokenspace/commands",
  "private": true,
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:types": "tsc --declaration --emitDeclarationOnly --outDir dist"
  },
  "dependencies": {
    "just-bash": "^2.10.2"
  },
  "devDependencies": {
    "@tokenspace/config": "workspace:*",
    "@types/bun": "^1.3.9",
    "typescript": "^5.8.3"
  }
}
