# `@tokenspace/types`

Type assets used by the Tokenspace compiler and editor surfaces.

## Install

```bash
bun add @tokenspace/types
```

## Usage

```ts
import { BUILTINS, MINIMAL_LIB, SANDBOX_TYPES } from "@tokenspace/types";
```
