{
  "name": "@tokenspace/executor",
  "private": true,
  "version": "0.1.0",
  "license": "Apache-2.0",
  "main": "./src/main.ts",
  "scripts": {
    "dev": "bun run --watch --no-clear-screen src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@tokenspace/backend": "workspace:*",
    "@tokenspace/compiler": "workspace:*",
    "@tokenspace/config": "workspace:*",
    "@tokenspace/sdk": "workspace:*",
    "@tokenspace/runtime-core": "workspace:*",
    "@tokenspace/session-fs": "workspace:*",
    "convex": "^1.33.0",
    "just-bash": "^2.13.0",
    "zod": "^4.3.5"
  }
}
